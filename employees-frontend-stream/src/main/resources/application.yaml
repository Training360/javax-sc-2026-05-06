server:
  port: 8080

spring:
  application:
    name: employees-frontend

logging:
  level:
    employees: DEBUG
    org.hibernate.SQL: TRACE
    org:
      springframework:
        core:
          retry: debug

employees:
  backend-url: http://localhost:8081